apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-kubernetes
  labels:
    app: httpd
spec:
  replicas: 2
  selector:
    matchLabels:
      app: httpd
  template:
    metadata:
      labels:
        app: httpd
    spec:
      containers:
      - name: httpd
        image: nginx:1.14.2
        ports:
        - containerPort: 80
